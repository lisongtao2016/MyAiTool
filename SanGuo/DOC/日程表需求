# 日程管理系统 - 需求规格说明书

## 1. 项目概述

### 1.1 项目背景
在大型项目管理中，传统的 HTML Table 或开源甘特图组件在处理超过 500 行数据或长达一年的时间轴时，往往会出现滚动卡顿、渲染延迟等问题。本项目旨在通过 **HTML5 Canvas** 技术，构建一个支持**固定列编辑、多维度信息合并展示、水平/垂直双向丝滑滚动**的专业日程表系统。

### 1.2 项目目标
- 实现高性能 Canvas 甘特图渲染（2000+ 行数据保持 60 FPS）
- 提供完整的 CRUD 操作（创建、读取、更新、删除任务）
- 支持实时搜索和过滤功能
- 提供数据导出能力（PNG/PDF）
- 确保跨浏览器兼容性

### 1.3 技术栈
- **前端**: HTML5 Canvas + 原生 JavaScript (ES6+)
- **后端**: Python 标准库 / .NET Framework 4.5+
- **数据库**: SQLite 3
- **通信**: RESTful API + CORS

---

## 2. 功能需求

### 2.1 界面布局与视觉规范

#### 2.1.1 整体布局
- **顶部表头 (Header)**: 高度 60px，深色背景 (#343a40)，文字白色，固定不动
- **左侧固定列 (Fixed Columns)**: 总宽度约 460px，包含项目基本信息
- **右侧滚动区 (Gantt/Timeline)**: 宽度自适应，展示日期轴及进度条
- **行高 (Row Height)**: 统一为 50px，以支持双行文字显示

#### 2.1.2 字段与合并规则

| 列名 | 内部字段 | 显示结构 | 宽度 | 是否可编辑 |
| :--- | :--- | :--- | :--- | :--- |
| **项目分级** | `group` / `subGroup` | 上下结构（大项目/中项目） | 120px | 是 |
| **小项目名** | `name` | 单行居中 | 150px | 是 |
| **期间** | `start_date` / `end_date` | 上下结构（开始/终了） | 110px | 是 |
| **进度(计/实)** | `plan` / `actual` | 上下结构（计划%/实际%） | 100px | 是 |
| **担当者** | `owner` | 单行居中 | 80px | 是 |

### 2.2 核心功能

#### 2.2.1 高性能 Canvas 渲染
- **虚拟滚动 (Virtual Scrolling)**: 仅渲染当前视口（Viewport）内的行
- **离线缓存**: 表头及静态装饰线在内存中预渲染，减少重绘时的 CPU 消耗
- **日期映射**: 逻辑坐标 X 与日期对象 Date 的转换算法，支持跨月份自动对齐
- **硬件加速**: 利用 Canvas 硬件加速特性提升渲染性能

#### 2.2.2 交互逻辑
- **平滑滚动**:
  - 直接拖拽：支持在 Canvas 任意区域按下鼠标左键拖动
  - 滚轮：上下滚动
  - Shift + 滚轮：水平左右滚动时间轴
- **单元格编辑**:
  - 点击左侧固定列，在该坐标处动态覆盖一个透明的 HTML Input 或 Datepicker
  - 失焦 (Blur) 或按下 Enter 键后，数据更新至内存数组并触发 requestAnimationFrame 重绘
- **实时搜索**:
  - 顶部搜索框支持按负责人姓名模糊搜索
  - 即时过滤并重新渲染视图
- **数据导出**:
  - 支持将当前 Canvas 内容导出为 PNG 图片
  - 保留完整样式和颜色

#### 2.2.3 进度条逻辑
- **底层 (计划)**: 浅灰色矩形，长度代表任务持续时长
- **上层 (实际)**:
  - 长度 = 计划长度 × 实际进度百分比
  - **颜色预警**:
    - 当 `实际 < 计划` 时显示蓝色 (#4a90e2)
    - 当 `实际 >= 计划` 时显示绿色 (#40c057)
    - 若任务逾期且进度未达 100%，显示红色 (#fa5252)

### 2.3 后端 API

#### 2.3.1 API 端点
- `GET /api/tasks` - 获取所有任务
- `GET /api/tasks/search?owner=xxx` - 按负责人搜索
- `POST /api/tasks` - 创建新任务
- `PUT /api/tasks/:id` - 更新任务
- `DELETE /api/tasks/:id` - 删除任务
- `GET /health` - 健康检查

#### 2.3.2 数据格式
```json
{
  "id": 1,
  "group": "大连工厂二期",
  "subGroup": "基础建设",
  "name": "地基灌浆施工",
  "startDate": "2026-04-06",
  "endDate": "2026-04-15",
  "startDay": 5,
  "duration": 10,
  "plan": 100,
  "actual": 85,
  "owner": "山田太郎"
}
```

---

## 3. 非功能性需求

### 3.1 性能要求
- **帧率**: 数据量在 2000 行以内时，滚动帧率需稳定在 **60 FPS**
- **加载时间**: 首屏加载时间（包含 1000 条数据处理）小于 **300ms**
- **响应时间**: API 请求响应时间小于 **100ms**

### 3.2 兼容性
- 支持 Chrome, Edge, Safari, Firefox 等主流浏览器
- 支持 Windows, macOS, Linux 操作系统

### 3.3 安全性
- 防止 SQL 注入（使用参数化查询）
- CORS 跨域配置
- 输入验证和 sanitization

### 3.4 可维护性
- 代码注释覆盖率 > 80%
- 模块化设计，便于扩展
- 完整的错误处理和日志记录

---

## 4. 数据库设计

### 4.1 表结构

#### tasks 表
| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INTEGER | 主键ID | PRIMARY KEY, AUTOINCREMENT |
| project_id | INTEGER | 项目ID（外键） | FOREIGN KEY → projects(id) |
| group_name | TEXT | 项目分组 | NOT NULL |
| sub_group | TEXT | 子模块 | NOT NULL |
| name | TEXT | 任务名称 | NOT NULL |
| start_date | DATE | 开始日期 | NOT NULL |
| end_date | DATE | 结束日期 | NOT NULL |
| start_day | INTEGER | 开始天数（甘特图偏移量） | NOT NULL, DEFAULT 0 |
| duration | INTEGER | 持续时间（天） | NOT NULL |
| plan | INTEGER | 计划进度（百分比） | NOT NULL, DEFAULT 100 |
| actual | INTEGER | 实际进度（百分比） | NOT NULL, DEFAULT 0 |
| owner | TEXT | 负责人 | NOT NULL |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间 | DEFAULT CURRENT_TIMESTAMP |

### 4.2 索引
- `idx_tasks_owner` - 按负责人搜索优化
- `idx_tasks_start_date` - 按开始日期查询优化
- `idx_tasks_end_date` - 按结束日期查询优化

---

## 5. 开发路线图

### Phase 1: 基础框架 ✅
- [x] Canvas 基本渲染
- [x] 虚拟滚动实现
- [x] 固定列布局

### Phase 2: 交互功能 ✅
- [x] 单元格编辑
- [x] 平滑滚动
- [x] 搜索过滤

### Phase 3: 后端集成 ✅
- [x] RESTful API
- [x] SQLite 数据库
- [x] CRUD 操作

### Phase 4: 增强功能
- [ ] 数据导出（PNG/PDF）
- [ ] 小地图导航
- [ ] 任务依赖关系
- [ ] 多项目切换

### Phase 5: 优化与测试
- [ ] 性能优化（2000+ 行）
- [ ] 单元测试
- [ ] 用户文档

---

## 6. 附录

### 6.1 术语表
- **Canvas**: HTML5 画布元素，用于高性能图形渲染
- **虚拟滚动**: 仅渲染可见区域的优化技术
- **甘特图**: 项目进度可视化的条形图

### 6.2 参考资料
- HTML5 Canvas API 文档
- SQLite 官方文档
- RESTful API 设计规范

---

**文档版本**: v2.0  
**最后更新**: 2026-04-08  
**状态**: 已完成基础功能，待增强